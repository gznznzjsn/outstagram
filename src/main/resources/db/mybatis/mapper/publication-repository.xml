<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.gznznzjsn.outstagram.persistence.repository.PublicationRepository">

    <resultMap id="PublicationMap" type="Publication" autoMapping="false">
        <id column="id" property="id"/>
        <result column="created_at" property="createdAt"/>
        <result column="is_pinned" property="isPinned"/>
        <association column="source_id" property="source" columnPrefix="source_"
                     resultMap="com.gznznzjsn.outstagram.persistence.repository.AccountRepository.AccountMap"
                     autoMapping="false"/>
        <association column="target_id" property="target" columnPrefix="target_"
                     resultMap="com.gznznzjsn.outstagram.persistence.repository.PostRepository.PostMap"
                     autoMapping="false"/>
    </resultMap>

    <insert id="create">
        match (a:ACCOUNT), (b:POST)
        where a.id = #{source.id} and b.id = #{target.id}
        create (a)-[:PUBLISHED {
                id: #{id},
                created_at: #{createdAt},
                is_pinned: #{isPinned}
        }]->(b);
    </insert>

</mapper>